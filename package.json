{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "npm run build",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
